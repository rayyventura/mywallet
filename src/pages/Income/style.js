import styled from "styled-components";

const Title = styled.h2`
  font-size: 26px;
  font-style: normal;
  font-weight: 700;
  line-height: 31px;
  letter-spacing: 0em;

  align-self: flex-start;

  margin-top: 25px;
  margin-bottom: 40px;
`;

export { Title };
